<UserControl x:Class="SimControlCentre.Views.Tabs.SettingsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left sidebar with categories -->
        <Border Grid.Column="0" 
                BorderBrush="LightGray" 
                BorderThickness="0,0,1,0"
                Padding="0,0,15,0">
            <StackPanel>
                <TextBlock Text="Settings" 
                          FontSize="18" 
                          FontWeight="Bold" 
                          Margin="0,0,0,20"/>
                
                <ListBox Name="CategoryListBox" 
                        SelectionChanged="CategoryListBox_SelectionChanged"
                        BorderThickness="0"
                        Background="Transparent">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="10,8"/>
                            <Setter Property="Margin" Value="0,0,0,5"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border Name="ItemBorder" 
                                                Background="Transparent"
                                                CornerRadius="4"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="ItemBorder" Property="Background" Value="LightBlue"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ItemBorder" Property="Background" Value="AliceBlue"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBoxItem Content="General" Tag="General"/>
                    <ListBoxItem Content="GoXLR" Tag="GoXLR"/>
                    <ListBoxItem Content="Controllers" Tag="Controllers"/>
                    <ListBoxItem Content="Logs" Tag="Logs"/>
                    <ListBoxItem Content="About" Tag="About"/>
                </ListBox>
            </StackPanel>
        </Border>

        <!-- Right panel with settings content -->
        <ScrollViewer Grid.Column="1" 
                     VerticalScrollBarVisibility="Auto"
                     Padding="20,0,0,0">
            <StackPanel Name="SettingsContent">
                <!-- Content will be dynamically loaded based on selection -->
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
